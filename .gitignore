# Build outputs
dist/
build/
__pycache__/
*.pyc
.DS_Store
*.spec

# Session notes (may contain personal info / local paths)
CLAUDE_SESSION.md
notarize.sh
*.zip

# Agent memory (local only, synced via cloud drive)
memory/
