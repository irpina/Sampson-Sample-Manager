# Build outputs
dist/
build/
__pycache__/
*.pyc
.DS_Store
*.spec

# Virtual environments (platform-specific)
.venv/
.venv_win/
.venv_mac/

# Session notes (may contain personal info / local paths)
CLAUDE_SESSION.md
notarize.sh
*.zip

# Agent memory (local only, synced via cloud drive)
memory/
