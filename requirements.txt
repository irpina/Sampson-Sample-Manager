# Core dependencies for SAMPSON v1.0.0
pygame-ce>=2.5.0
pydub>=0.25.1
static-ffmpeg>=2.5.0
pywebview>=4.0

# Qt backend for Windows/Linux (macOS uses native cocoa backend)
pywebview[qt]>=4.0; sys_platform == "linux"

# Python 3.13+ compatibility (audioop was removed from stdlib)
audioop-lts>=0.2.0; python_version >= "3.13"

# macOS-specific (only installed on Darwin)
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
